<html>
    <div>    
        <table class="row-border" frame="void" rules="rows" cellspacing="10">
                        <tr>
                            <th colspan=5>Routes</th>
                        </tr>

                        <% @routes.each do |routes| %>
                            <tr>
                                <td>
                                    <%= area.location %>  <%= area.location %>
                                </td>

                                <td><%= link_to edit_route_path([:id]), class: "btn btn-default" do %>
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> <% end %>
                                </td>

                                <td><%= link_to route_path(area[:id]), class: "btn btn-default", method: :delete, data: {confirm: 'Are you sure'} do %>
                                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
                                    <% end %>
                                </td>    

                            </tr>
                        <% end %>

            </table>
     </div>    
</html>